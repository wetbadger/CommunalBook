{
  "scripts": {
    "test": "vitest run --config vitest.backend.config.js",
    "test:watch": "vitest --config vitest.backend.config.js",
    "test:coverage": "vitest run --config vitest.backend.config.js --coverage",
    "test:ui": "vitest --ui --config vitest.backend.config.js"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.1",
    "@vitest/ui": "^1.6.1",
    "supertest": "^6.3.4",
    "vitest": "^1.6.1"
  }
}
